[target.thumbv7m-none-eabi]
rustflags = [
  "--C", "relocation-model=static",
  "--C", "target-feature=+thumb2",
  "--C", "target-cpu=cortex-m3",
  "--C", "panic=abort",
]

[profile.test]
# Tests share global state (DB_INSTANCE, GLOBAL_ALLOCATOR) so must run single-threaded
test-threads = 1
